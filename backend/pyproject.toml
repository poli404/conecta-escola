[tool.poetry]
name = "conecta-escola"
version = "0.1.0"
description = ""
authors = ["anapaulacrippa <aplcrippa@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pyjwt = "^2.10.1"
pwdlib = {extras = ["argon2"], version = "^0.3.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
